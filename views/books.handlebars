<h1>Meus Livros</h1>

<div class="books-container">
<a href="/">Adicionar Livros</a>
{{#each books}}
<div>
    # {{this.id}} - <span class="bold" >{{this.title}}</span > - {{this.nm_paginas}} páginas
 </div>
{{/each}}
</div>